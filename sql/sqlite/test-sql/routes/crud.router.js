// const router = require("express").Router();
// const { getItems, createItem } = require("../controller/controller");

// router.route("/item").get(getItems).post(createItem);

// module.exports = router;
